export class Tile {
    constructor(data, title, url, post_url) {
        this.data = data;
        this.title = title;
        this.url = url;
        this.post_url = post_url;
    }
}

export default Tile;